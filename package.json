{
  "name": "@traffic-source/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "ts": "dist/ts.mjs"
  },
  "files": [
    "dist/ts.mjs"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "bun run build.ts",
    "dev": "bun --watch run src/main.ts --",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "@types/node": "^20",
    "typescript": "^5"
  }
}
